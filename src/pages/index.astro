---
import Base from '../layouts/Base.astro';
import Site from '../components/Site.astro';
import { ui } from '../i18n/ui.js';
const lang = 'en';
---
<Base lang={lang} title={ui[lang]['doc.title']} description="Embrace embeds trained ecommerce operators inside your organization — building the strategy, running the systems, and owning online revenue in Ecuador.">
  <Site lang={lang} />
</Base>
